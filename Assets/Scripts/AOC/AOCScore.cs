using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AOCScore : Singleton<AOCScore>
{

	public enum DayScore{ NoStar, OneStar, TwoStars};
	public DayScore[] Scores;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
